enum Token { value(int) }

func classify(token:Token) int {
    return match token {
        value(_) => 1
//            ^ variable.special
    }
}

func compile_digits() {
    let digits = try Regex.compile("\\d+")
        else error "the digit pattern is invalid"
//      ^^^^ keyword
//           ^^^^^ variable
}
